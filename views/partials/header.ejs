<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title><%= title || name %></title>

<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons+Outlined" rel="stylesheet">

<style>
:root{
  --bg:#020c10;
  --panel:#06171c;
  --card:#0b1f26;
  --border:rgba(255,255,255,.06);
  --text:#e6f7ff;
  --muted:#9cc9d9;
  --accent:#4fd1ff;
}
*{box-sizing:border-box}
body{
  margin:0;
  font-family:Inter,system-ui;
  background:linear-gradient(180deg,#041b22,var(--bg));
  color:var(--text);
}
.topbar{
  height:56px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:0 16px;
  background:#041c22;
  border-bottom:1px solid var(--border);
}
.topbar button{
  background:none;border:none;color:var(--accent)
}
#overlay{
  position:fixed;inset:0;
  background:rgba(0,0,0,.5);
  display:none;z-index:9
}
#overlay.show{display:block}
#drawer{
  position:fixed;top:0;left:0;
  width:260px;height:100%;
  background:#041c22;
  transform:translateX(-100%);
  transition:.35s ease;
  z-index:10;padding:16px
}
#drawer.show{transform:translateX(0)}
#drawer a{
  display:block;
  padding:12px;
  border-radius:12px;
  color:var(--text);
  text-decoration:none
}
#drawer a:hover{background:rgba(255,255,255,.06)}
main{
  max-width:1100px;
  margin:auto;
  padding:20px
}
.card{
  background:var(--card);
  border:1px solid var(--border);
  border-radius:16px;
  padding:16px;
}
.method{
  padding:4px 10px;
  border-radius:999px;
  font-size:12px;
  font-weight:700;
}
.method.get{background:rgba(79,209,255,.15);color:var(--accent)}
.method.post{background:rgba(255,180,100,.15);color:#ffb464}
.endpoint-head{
  display:flex;align-items:center;gap:12px;
  cursor:pointer
}
.endpoint-body{
  display:none;
  margin-top:12px
}
.endpoint-body.open{display:block}
input{
  width:100%;
  padding:12px;
  border-radius:12px;
  background:#04141a;
  border:1px solid var(--border);
  color:var(--text)
}
button.exec{
  margin-top:12px;
  padding:10px 18px;
  border-radius:14px;
  border:none;
  background:var(--accent);
  color:#001017;
  font-weight:700;
  cursor:pointer
}
pre{
  background:#031017;
  border-radius:14px;
  padding:12px;
  margin-top:12px;
  font-size:12px;
  max-height:300px;
  overflow:auto
}
</style>
</head>

<body>

<header class="topbar">
  <button onclick="toggleMenu()">
    <span class="material-icons-outlined">menu</span>
  </button>
  <b><%= name %></b>
  <% if(showGetStarted!==false){ %>
    <a href="/api" style="color:var(--accent)">Get Started</a>
  <% } %>
</header>

<%- include('sidebar',{name}) %>

<script>
function toggleMenu(){
  drawer.classList.toggle('show')
  overlay.classList.toggle('show')
}
</script>

<main>