<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title><%= title || name %></title>

<style>
:root{
  --bg:#001011;
  --card:#0b1c1f;
  --border:rgba(255,255,255,.08);
  --text:#e6f6fb;
  --muted:#9bb7c3;
  --accent:#6ccff6;
}

/* RESET */
*{box-sizing:border-box}
body{
  margin:0;
  font-family:Inter,system-ui,sans-serif;
  background:radial-gradient(circle at top,#062128,var(--bg));
  color:var(--text);
}

/* TOPBAR */
.topbar{
  height:60px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  padding:0 1rem;
  background:rgba(0,16,17,.8);
  border-bottom:1px solid var(--border);
}

/* LAYOUT */
.app{
  display:grid;
  grid-template-columns:240px 1fr 320px;
  height:calc(100vh - 60px);
}

/* SIDEBAR */
.sidebar{
  padding:.8rem;
  overflow:auto;
  background:linear-gradient(180deg,#0e2429,#071417);
  border-right:1px solid var(--border);
}

/* KATEGORI ITEM */
.cat{
  padding:.6rem .8rem;
  border-radius:12px;
  cursor:pointer;
  color:var(--muted);
  transition:.25s;
}
.cat:hover,
.cat.active{
  background:rgba(108,207,246,.12);
  color:var(--accent);
}

/* ENDPOINT ITEM */
.endpoint{
  padding:.6rem;
  border-radius:10px;
  font-size:.85rem;
  cursor:pointer;
  transition:.25s;
}
.endpoint:hover,
.endpoint.active{
  background:rgba(255,255,255,.08);
}

/* CONTENT */
.content{
  padding:1rem;
  overflow:auto;
  animation:fade .4s ease;
}

/* CARD */
.card{
  background:linear-gradient(180deg,#0e2429,#09181b);
  border:1px solid var(--border);
  border-radius:16px;
  padding:1rem;
  margin-bottom:1rem;
}

/* ANIMATION */
@keyframes fade{
  from{opacity:0;transform:translateY(6px)}
  to{opacity:1}
}
</style>
</head>

<body>

<div class="topbar">
  <b><%= name %></b>
  <a href="/api" style="color:var(--accent)">Get Started</a>
</div>

<div class="app">